{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    {{ encore_entry_link_tags('form') }}
{% endblock %}

{% block title %}{{ 'createFromCSV'|trans }}{% endblock %}

{% block body %}
    {{ form_start(form, {'attr': {'class': 'centeredForm'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
