<?php
/**
 * Make IDE type-hint correctly the return type
 */

namespace PHPSTORM_META {

    // use xVer\MiCartera\Application\RepositoryFactory;

    // override(RepositoryFactory::assignRepository(0), map([''=>'@']));
}